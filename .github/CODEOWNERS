# Ownership. GitHub auto-requests a review from the owner of every path a PR
# touches, so a PR that crosses a boundary is visibly crossing it.
#
# A handle that does not resolve is skipped in silence, so every name here has
# to be a real collaborator with write access — otherwise the file looks like
# it is protecting a path while doing nothing at all.

# Core platform. Nobody else edits these.
*                                   @Kartikkulk
/apps/api/app/services/             @Kartikkulk
/apps/api/app/api/                  @Kartikkulk
/apps/api/app/llm/                  @Kartikkulk
/apps/web/                          @Kartikkulk
/collectors/shared/                 @Kartikkulk
/contracts/                         @Kartikkulk

# ── domain packs · one file each, no core changes needed ──────────────────
/apps/api/app/domains/finance.py            @AnirudhZalki
/apps/api/app/domains/customer_support.py   @AnirudhZalki
/apps/api/app/domains/sales.py              @Vijayavardhan-sarasambekar
/apps/api/app/domains/hr.py                 @Vijayavardhan-sarasambekar

# ── browser collectors · one folder each ──────────────────────────────────
# Anushree's handle is still unknown, so this path deliberately has no owner
# rather than a name that would resolve to nobody and quietly review nothing.
/collectors/chrome/
/collectors/edge/                   @Gouri066
